# =========================================================================
# COMPSSA Website — environment variables (template)
# Copy to .env.local and fill in real values. NEVER commit .env files.
# =========================================================================

# --- Supabase (planned for Phase 2 / production) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Public site config ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DEPARTMENT_NAME="Department of Computer Science"